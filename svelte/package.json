{
  "name": "svelte",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json",
    "extract": "wuchale",
    "clean": "wuchale --clean"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.1.2",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.34.7",
    "svelte-check": "^4.3.1",
    "typescript": "~5.9.2",
    "vite": "^7.0.6"
  },
  "dependencies": {
    "@wuchale/svelte": "^0.12.0",
    "@wuchale/vite-plugin": "^0.12.0",
    "wuchale": "^0.13.0"
  }
}
